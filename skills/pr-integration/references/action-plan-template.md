# PR Integration Action Plan Artefact Template

## Purpose

This file is a generated artefact from the `audit` stage of the PR integration skill.

- It captures repo state, PR analysis, and the exact proposed execution plan.
- It is reviewed and approved before `execute` mode runs.
- A new copy is generated for each integration run.

## Artefact Naming

Generate per run in:

- `docs/integration-runs/<YYYY-MM-DD>-<batch>-action-plan.md`
- `docs/integration-runs/<YYYY-MM-DD>-<batch>-action-plan.json`

## Run Metadata

- Run date: `<YYYY-MM-DD>`
- Repository: `<owner>/<repo>`
- Mode: `audit`
- Requested by: `<user>`
- Generated by: `<agent/version>`

## Main Snapshot

- Current branch: `<branch>`
- Current `main` head: `<sha> (<subject>)`
- Working tree clean: `<yes/no>`
- Audit started from `main`: `<yes/no>`
- Branch protection: `<summary>`
- Allowed merge methods: `<merge/squash/rebase>`

## PR Inventory

1. `#<number>` `<title>`
   - Branch: `<headRefName>` -> `<baseRefName>`
   - CI status: `<checks summary>`
   - Review status: `<reviewDecision>`
   - Labels: `<labels>`
   - Changed files: `<count>`

## Scoring

Use the defined model:

| Signal | Score |
|---|---:|
| Security fix | +100 |
| Bug fix | +60 |
| Performance change | +40 |
| UX enhancement | +30 |
| Documentation-only | +10 |
| Failing CI | -80 |
| Overlap/conflict risk with higher-priority PR | -20 |

PR scores:

1. `#<number>`: `<score>`
2. `#<number>`: `<score>`
3. `#<number>`: `<score>`

## Conflict Matrix

| File | PRs touching file | Conflict risk |
|---|---|---|
| `<path>` | `#<a>, #<b>` | `<high/medium/low>` |

Conflict resolution notes:

1. `<rule applied>`
2. `<rule applied>`

## Allowed-File Guardrail Check

For each PR, compare expected vs actual touched files.

1. `#<number>`
   - Expected: `<paths>`
   - Unexpected: `<paths or none>`
   - Decision: `<exclude/keep with approval>`

## Required Fixes Before Merge

1. `#<number>`: `<required code fix>`
2. `#<number>`: `<required docs fix>`
3. `#<number>`: `<required standards fix>`

## Label Plan

1. `#<number>` -> `<label1>, <label2>`
2. `#<number>` -> `<label1>, <label2>, <label3>`

## PR Title Checks

For each open PR in the stack:

1. Title starts with a capital letter.
2. Title does not use Conventional Commit prefixes (for example, no `feat:`, `fix:`, `chore:`).
3. Title describes outcome or behaviour change.
4. Title style is consistent across the open stack.

## PR Description Checks

For each open PR in the stack:

1. Description uses outcome-focused language.
2. Description does not mention deferred-review documents, internal queue labels, or internal-only planning notes.
3. Internal process details are only included when explicitly requested.

## Mergeability Checks

For each open PR in the stack:

1. Merge state: `<clean/dirty/blocked>`
2. If `DIRTY`, rebase branch onto `origin/main` before merge and rerun checks.
3. Record conflict resolution notes for touched overlapping files.

## Merge Order Proposal

1. `#<number>` `<why first>`
2. `#<number>` `<why second>`
3. `#<number>` `<why third>`

## Planned Commands (Dry-Run)

```bash
# Preflight
<commands>

# Branch setup
<commands>

# Per-PR integration
<commands>

# Validation
pnpm test
pnpm build
```

Run critical git operations sequentially (no parallel execution) for:

- `git switch`
- `git merge`
- `git rebase`
- `git cherry-pick`

## Execution Gates

All must pass before `execute` mode:

1. Preflight checks pass.
2. CI checks pass on latest PR heads.
3. Required review policy passes.
4. Working tree is clean.
5. Plan approved by reviewer.
6. Push approval explicitly granted by user.
7. Audit started on `main`.

## Post-Merge Finalization

After all merges complete:

1. `git switch main`
2. `git pull --ff-only`
3. `pnpm test`
4. `pnpm build`
5. Record final status in run summary.

## Merge Message Format (Required)

```text
PR Title (#PRNo.)

PR Description
```

## Commit Message Format (Required)

Use Conventional Commits with bodies that explain what and why.

## Rollback Plan

If any gate fails in execution:

1. Stop execution.
2. Revert the last integration commit on integration branch.
3. Re-run `pnpm test` and `pnpm build`.
4. Regenerate this artefact with updated state.

## Approval

- Reviewer: `<name>`
- Decision: `<approved/changes requested>`
- Timestamp: `<ISO-8601>`
- Notes: `<optional>`
